# Four-row landscape display fixture; physical inputs use the same controller.
substitutions:
  compact_profile: tft160
packages:
  base: !include compact.yaml
  colors: !include ../components/compact-shell/color.yaml
esphome:
  name: nabla-tft160
