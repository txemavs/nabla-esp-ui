# Readable host fixture; exercises the tiny renderer with the larger single-item presentation.

packages:
  main: !include tiny.yaml
substitutions:
  compact_profile: readable
esphome:
  name: nabla-readable
